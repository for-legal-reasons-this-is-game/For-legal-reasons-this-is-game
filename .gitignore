# Rust build output. Unanchored so it matches every crate in the repo
# (backend/, frontend/, tradingengine/) rather than one at a time.
target/
node_modules/

# Nix build results and the direnv cache
result
result-*
.direnv/

# Environment and secrets.
.env
.env.*
!.env.example

# Editors and OS
.vscode/
.idea/
*.swp
